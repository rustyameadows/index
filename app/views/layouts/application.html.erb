<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Index" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="site-header" style="padding: 1rem 1.5rem; border-bottom: 1px solid #ddd; display: flex; justify-content: space-between; align-items: center;">
      <div class="brand">
        <%= link_to "Index", root_path, style: "text-decoration:none; font-weight:700; color:#222;" %>
      </div>
      <nav class="user-nav" style="display:flex; gap: 0.75rem; align-items:center;">
        <% if logged_in? %>
          <span style="color:#555;">Signed in as <%= current_user.email %></span>
          <%= button_to "Sign out", session_path, method: :delete, form: { style: "display:inline" }, class: "link-button" %>
        <% else %>
          <%= link_to "Sign in", new_session_path %>
          <%= link_to "Sign up", new_user_path %>
        <% end %>
      </nav>
    </header>

    <main style="padding: 1.5rem;">
      <% flash.each do |type, message| %>
        <div class="flash flash-<%= type %>" style="margin-bottom: 1rem; padding: 0.75rem 1rem; border: 1px solid #ddd; background: #f8f9fa;">
          <%= message %>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
